# Experiment Tracking

Each training run creates a timestamped directory here.

Structure:
- experiments/YYYY-MM-DD_HHMM/config.yaml (snapshot of config used)
- experiments/YYYY-MM-DD_HHMM/metadata.json (timestamp, git commit, command)

Note: These directories are tracked in git for documentation purposes.
