# NOTE: On ROCm/CUDA systems, install torch/torchvision/torchaudio using
# your platform-specific instructions, then install the remaining deps here.

# Core dependencies for dataset generation
transformers==4.50.0  # For DINOv3, T5 models
pillow==10.4.0
accelerate==0.34.2
safetensors==0.4.5
diffusers==0.30.3  # Pinned to avoid torchao logger bug in 0.36.0+

# For Ollama API (caption generation)
requests
